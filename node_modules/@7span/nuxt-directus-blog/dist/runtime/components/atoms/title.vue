<template>
  <div>
    <slot :data="{ title }">
      <component :is="as">
        {{ title }}
      </component>
    </slot>
  </div>
</template>

<script setup>
import { inject } from "#imports";

const { title } = inject("db-article-data");
const { as } = defineProps({
  as: {
    type: String,
    default: "p"
  }
});
</script>
