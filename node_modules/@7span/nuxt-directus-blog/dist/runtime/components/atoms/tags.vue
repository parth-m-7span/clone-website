b<template>
  <div>
    <slot :data="{ tags: _tags }">
      {{ _tags }}
    </slot>
  </div>
</template>

<script setup>
import { inject } from "#imports";

const { tags } = inject("db-article-data");

const _tags = tags.map(tag => tag.bg_tags_id);
</script>
