<template>
  <div>
    <slot :data="{ url }">
      <img :src="url">
    </slot>
  </div>
</template>

<script setup>
import { useRuntimeConfig, inject } from "#imports";

const {
  cover: { filename_disk: fileNameDisk },
} = inject("db-article-data");
const {
  public: {
    blog: { assetsBaseUrl },
  },
} = useRuntimeConfig();

const url = `${assetsBaseUrl}/${fileNameDisk}`;
</script>
