<template>
  <div>
    <slot :data="{ data }" />
  </div>
</template>

<script setup>
import { useDbArticle } from "../composables/useDbArticle";
import { provide, useHead } from "#imports";
const { data } = await useDbArticle();

provide("db-article-data", data);

useHead({
  title: data.title,
});
</script>
