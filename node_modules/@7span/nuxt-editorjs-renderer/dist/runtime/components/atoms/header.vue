<template>
  <slot name="header">
    <component :is="tag" class="ejr__header">
      <span v-html="props.value.data.text"> </span>
    </component>
  </slot>
</template>

<script setup>
const props = defineProps({
  value: {
    type: Object,
    default: () => {},
  },
});

const tag = `h${props.value.data.level}`;
</script>
